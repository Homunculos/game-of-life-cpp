# Conway's Game of Life (C++)

پیاده‌سازی ساده‌ای از **بازی زندگی Conway (Game of Life)** با زبان ++C به‌صورت کنسولی.

این برنامه یک جهان دوبعدی را به‌صورت تصادفی مقداردهی کرده و سپس با اعمال قوانین Game of Life، تکامل سلول‌ها را به‌صورت انیمیشن در ترمینال نمایش می‌دهد.

---

## 📌 ویژگی‌ها

- پیاده‌سازی کامل Game of Life
- نمایش گرافیکی ساده در کنسول
- مقداردهی اولیه تصادفی
- استفاده از آرایه دوبعدی
- قابل اجرا روی **Windows**

---

## 🧠 قوانین Game of Life

برای هر سلول:

- اگر زنده باشد:
  - با کمتر از ۲ همسایه زنده → می‌میرد
  - با ۲ یا ۳ همسایه زنده → زنده می‌ماند
  - با بیشتر از ۳ همسایه زنده → می‌میرد
- اگر مرده باشد:
  - با دقیقاً ۳ همسایه زنده → زنده می‌شود

---

## 🛠️ پیش‌نیازها

- کامپایلر ++C (مثل `g++`)
- سیستم‌عامل **Windows**  
  (به دلیل استفاده از `windows.h` و `Sleep`)

---

## ▶️ نحوه اجرا

1. کامپایل برنامه:
   ```bash
   g++ main.cpp -o game
